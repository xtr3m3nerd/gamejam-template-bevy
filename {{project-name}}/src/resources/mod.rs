use bevy::prelude::*;

#[derive(Default)]
pub struct Game {
    // pub current_level : i32,
    // pub levels: Vec<GameLevel>,
    // pub sacrifices: i32,
    // pub deaths: i32,
    // pub has_koolaid: bool,
    // pub tooltip: String,
    // pub is_at_koolaid_stand: bool,
}
